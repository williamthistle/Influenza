innate_deg_heatmap_table <- data.frame("CD16 Mono" = numeric(), "CD14 Mono" = numeric(), "cDC" = numeric(), "pDC" = numeric(), "NK" = numeric())

for(cell_type in colnames(innate_deg_heatmap_table)) {
  
}

